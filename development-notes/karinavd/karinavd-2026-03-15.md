**Date:** 2026-03-15

**What was done:**
   - Created and added 6 unit tests for checking whether validation works and validation errors are displayed correctly, is Back button redirect to Welcome page
   - Started adding internalization for the application layout.
     
   **Code Review**
   
   Analyzed the "About Us" section components ('AboutUsTop', 'AboutCard', 'AboutHero') written by my teammate @Alcopatriarch:
   - [PR #82: add a page about us](https://github.com/Alcopatriarch/RS-Tandem/pull/82) - added 7 comments:
       1. 3 out of 7 comments suggested adding unit tests to verify that components render correctly;
       2. 2 out of 7 comments suggested changing the `className` to improve styling
       3. 1 comment suggested moving the data array to a separate folder so it isn't stored in `types`, and one comment asked what the `div` is for

**Problems:** 
During the code review, I reflected heavily on project architecture. Storing data arrays inside type definition files is a common anti-pattern that mixes static typing with actual data

**Solution**
In the PR review, I focused on providing constructive and specific feedback, explaining why moving the data to a separate constants/data folder improves the project's maintainability

**Thoughts / Plans:**
- Add internalization to pages
- Start working on another component(test a,b,c task)
  
**Time spent:** 3 hours
